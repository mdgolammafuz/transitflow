version: '3.8'

# --- Global Logging Configuration (Prevents Disk Full) ---
x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

# --- Shared Environment Variables ---
x-app-env: &app-env
  KAFKA_BOOTSTRAP_SERVERS: "redpanda:29092"
  POSTGRES_HOST: "postgres"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "${POSTGRES_DB}"
  POSTGRES_USER: "${POSTGRES_USER}"
  POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
  REDIS_HOST: "redis"
  REDIS_PORT: "6379"
  REDIS_PASSWORD: "${REDIS_PASSWORD}"
  MINIO_ROOT_USER: "${MINIO_ROOT_USER}"
  MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD}"
  METRICS_PORT: "8000"

services:
  # --- Ingestion Service ---
  producer:
    image: python:3.10-slim
    container_name: producer
    working_dir: /app
    dns:
      - 8.8.8.8
      - 8.8.4.4
    logging: *default-logging
    environment:
      <<: *app-env
      PYTHONPATH: /app
      KAFKA_BOOTSTRAP_SERVERS: "redpanda:29092" 
    volumes:
      - ../../:/app
    command: >
      /bin/bash -c "pip install --no-cache-dir requests confluent-kafka fastavro python-dotenv pydantic pydantic-settings prometheus-client paho-mqtt &&
      python scripts/run_bridge.py --line 600"
    depends_on:
      redpanda:
        condition: service_healthy
    restart: always

  # --- Real-Time Inference API ---
  serving-api:
    build:
      context: ../../
      dockerfile: docker/Dockerfile.serving
    container_name: serving-api
    restart: always
    working_dir: /app
    ports:
      - "8001:8001"
    dns:
      - 8.8.8.8
    logging: *default-logging
    environment:
      <<: *app-env
      # Connect to Cloud Infra
      MLFLOW_TRACKING_URI: http://mlflow:5000
      MLFLOW_S3_ENDPOINT_URL: http://minio:9000
      AWS_ACCESS_KEY_ID: ${MINIO_ROOT_USER}
      AWS_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD}
      AWS_DEFAULT_REGION: us-east-1
      # Enable Shadow Mode for safety
      ENABLE_SHADOW: "true"
      # Point to containers
      FEATURE_API_URL: http://feature-api:8000
      OTLP_ENDPOINT: http://jaeger:4317
    volumes:
      - ../../:/app
    depends_on:
      - redis
      - postgres
      - mlflow

  # --- Message Broker ---
  redpanda:
    image: redpandadata/redpanda:v23.3.3
    container_name: redpanda
    command:
      - redpanda start
      - --smp 1
      - --memory 1G
      - --reserve-memory 0M
      - --overprovisioned
      - --node-id 0
      - --check=false
      - --kafka-addr PLAINTEXT://0.0.0.0:29092,OUTSIDE://0.0.0.0:9092
      - --advertise-kafka-addr PLAINTEXT://redpanda:29092,OUTSIDE://127.0.0.1:9092
      - --schema-registry-addr 0.0.0.0:8081
    ports:
      - "9092:9092"
      - "9644:9644"
      - "8081:8081"
    logging: *default-logging
    volumes:
      - redpanda_data:/var/lib/redpanda/data
    healthcheck:
      test: ["CMD", "rpk", "cluster", "health"]
      interval: 30s
      timeout: 30s
      retries: 10
      start_period: 60s

  redpanda-console:
    image: redpandadata/console:v2.4.3
    container_name: redpanda-console
    environment:
      KAFKA_BROKERS: redpanda:29092
      SCHEMA_REGISTRY_ENABLED: "true"
      SCHEMA_REGISTRY_URLS: http://redpanda:8081
    ports:
      - "8080:8080"
    logging: *default-logging
    depends_on:
      redpanda:
        condition: service_healthy

  # --- Stream Processing (Flink) ---
  flink-jobmanager:
    image: flink:1.18.1-java11
    container_name: flink-jobmanager
    ports:
      - "8082:8081"
    dns:
      - 8.8.8.8
      - 8.8.4.4
    logging: *default-logging
    command: >
      /bin/sh -c "
      mkdir -p /opt/flink/plugins/s3-fs-hadoop;
      cp /opt/flink/opt/flink-s3-fs-hadoop-1.18.1.jar /opt/flink/plugins/s3-fs-hadoop/;
      /docker-entrypoint.sh jobmanager
      "
    environment:
      <<: *app-env
      KAFKA_BOOTSTRAP_SERVERS: "redpanda:29092"
      REDIS_HOST: "redis"
      REDIS_PASSWORD: "${REDIS_PASSWORD}"
      MINIO_ENDPOINT: "http://minio:9000"
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: flink-jobmanager
        s3.endpoint: http://minio:9000
        s3.path.style.access: true
        s3.access-key: ${MINIO_ROOT_USER}
        s3.secret-key: ${MINIO_ROOT_PASSWORD}
        state.checkpoints.dir: s3://flink-checkpoints/data/
        execution.checkpointing.interval: 60s
    volumes:
      - ./flink-jobs:/opt/flink/jobs
    depends_on:
      redpanda:
        condition: service_healthy
      minio:
        condition: service_healthy

  flink-taskmanager:
    image: flink:1.18.1-java11
    container_name: flink-taskmanager
    dns:
      - 8.8.8.8
      - 8.8.4.4
    logging: *default-logging
    command: >
      /bin/sh -c "
      mkdir -p /opt/flink/plugins/s3-fs-hadoop;
      cp /opt/flink/opt/flink-s3-fs-hadoop-1.18.1.jar /opt/flink/plugins/s3-fs-hadoop/;
      /docker-entrypoint.sh taskmanager
      "
    environment:
      <<: *app-env
      KAFKA_BOOTSTRAP_SERVERS: "redpanda:29092"
      REDIS_HOST: "redis"
      REDIS_PASSWORD: "${REDIS_PASSWORD}"
      MINIO_ENDPOINT: "http://minio:9000"
      FLINK_PROPERTIES: |
        jobmanager.rpc.address: flink-jobmanager
        taskmanager.numberOfTaskSlots: 4
        s3.endpoint: http://minio:9000
        s3.path.style.access: true
        s3.access-key: ${MINIO_ROOT_USER}
        s3.secret-key: ${MINIO_ROOT_PASSWORD}
    depends_on:
      - flink-jobmanager

  # --- Model Tracking ---
  mlflow:
    image: python:3.9-slim
    container_name: mlflow
    ports:
      - "5001:5000"
    dns:
      - 8.8.8.8
      - 8.8.4.4
    logging: *default-logging
    environment:
      AWS_ACCESS_KEY_ID: ${MINIO_ROOT_USER}
      AWS_SECRET_ACCESS_KEY: ${MINIO_ROOT_PASSWORD}
      MLFLOW_S3_ENDPOINT_URL: http://minio:9000
    volumes:
      - ../../mlruns:/mlflow
    command: >
      /bin/bash -c "pip install --no-cache-dir mlflow==2.10.2 boto3 && 
      mlflow server --host 0.0.0.0 --port 5000 
      --backend-store-uri sqlite:///mlflow/mlflow.db 
      --default-artifact-root s3://transitflow-lakehouse/mlflow"
    depends_on:
      minio:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 5

  # --- Batch Processing & ML Training (Spark) ---
  spark-master:
    image: apache/spark:3.5.0
    container_name: spark-master
    user: root
    dns:
      - 8.8.8.8
      - 8.8.4.4
    logging: *default-logging
    environment:
      <<: *app-env
      KAFKA_BOOTSTRAP_SERVERS: "redpanda:29092"
      REDIS_HOST: "redis"
      POSTGRES_HOST: "postgres"
      MINIO_ENDPOINT: "http://minio:9000"
      SPARK_MODE: master
      PYSPARK_PYTHON: python3
      PYTHONPATH: /opt/spark/jobs
      MLFLOW_TRACKING_URI: http://mlflow:5000
    command: >
      /bin/bash -c "pip3 install --no-cache-dir psycopg2-binary 'xgboost<2.0.0' structlog scikit-learn pandas mlflow-skinny pytest pytest-spark python-dotenv pydantic pydantic-settings fastavro && /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master"
    volumes:
      - ../../spark:/opt/spark/jobs/spark
      - ../../feature_store:/opt/spark/jobs/feature_store
      - ../../src:/opt/spark/jobs/src
      - ../../scripts:/opt/spark/jobs/scripts
      - ../../ml_pipeline:/opt/spark/jobs/ml_pipeline
      - ../../tests:/opt/spark/jobs/tests
      - ../../infra:/opt/spark/jobs/infra
      - ../../dbt:/opt/spark/jobs/dbt
      - ../../serving:/opt/spark/jobs/serving
    ports:
      - "7077:7077"
      - "8083:8080"
    depends_on:
      - mlflow

  spark-worker:
    image: apache/spark:3.5.0
    container_name: spark-worker
    user: root
    dns:
      - 8.8.8.8
      - 8.8.4.4
    logging: *default-logging
    environment:
      <<: *app-env
      KAFKA_BOOTSTRAP_SERVERS: "redpanda:29092"
      REDIS_HOST: "redis"
      POSTGRES_HOST: "postgres"
      MINIO_ENDPOINT: "http://minio:9000"
      SPARK_MODE: worker
      SPARK_MASTER_URL: spark://spark-master:7077
      SPARK_WORKER_MEMORY: 2G
      PYSPARK_PYTHON: python3
      PYTHONPATH: /opt/spark/jobs
      MLFLOW_TRACKING_URI: http://mlflow:5000
    command: >
      /bin/bash -c "pip3 install --no-cache-dir psycopg2-binary 'xgboost<2.0.0' structlog scikit-learn pandas mlflow-skinny python-dotenv pydantic pydantic-settings fastavro && /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077"
    volumes:
      - ../../spark:/opt/spark/jobs/spark
      - ../../feature_store:/opt/spark/jobs/feature_store
      - ../../src:/opt/spark/jobs/src
      - ../../scripts:/opt/spark/jobs/scripts
      - ../../ml_pipeline:/opt/spark/jobs/ml_pipeline
      - ../../tests:/opt/spark/jobs/tests
      - ../../infra:/opt/spark/jobs/infra
      - ../../dbt:/opt/spark/jobs/dbt
      - ../../serving:/opt/spark/jobs/serving
    depends_on:
      - spark-master
      - mlflow

  # --- Storage (MinIO Server) ---
  minio:
    image: minio/minio:RELEASE.2024-01-13T07-53-03Z
    container_name: minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    ports:
      - "9000:9000"
      - "9001:9001"
    logging: *default-logging
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "/usr/bin/mc", "ready", "local"]
      interval: 10s
      timeout: 10s
      retries: 5

  # --- Storage Initialization (Worker) ---
  minio-init:
    image: minio/mc:RELEASE.2024-01-13T08-44-48Z
    container_name: minio-init
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MC_HOST_myminio: http://${MINIO_ROOT_USER}:${MINIO_ROOT_PASSWORD}@minio:9000
    logging: *default-logging
    entrypoint: >
      /bin/sh -c "
      # Create primary bucket for Data Lakehouse & MLflow artifacts
      mc mb myminio/transitflow-lakehouse --ignore-existing;
      mc mb myminio/flink-checkpoints --ignore-existing;
      
      # Set policies to allow MLflow client and Spark jobs to access buckets
      mc policy set public myminio/transitflow-lakehouse;
      mc policy set public myminio/flink-checkpoints;
      
      exit 0;
      "

  postgres:
    image: postgres:15-alpine
    container_name: postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    logging: *default-logging
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ${PWD}/init/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: redis
    command: redis-server --requirepass ${REDIS_PASSWORD}
    ports:
      - "6379:6379"
    logging: *default-logging
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  dbt:
    image: python:3.10-slim
    container_name: dbt
    working_dir: /usr/app
    dns:
      - 8.8.8.8
      - 8.8.4.4
    logging: *default-logging
    environment:
      <<: *app-env
    volumes:
      - ../../:/usr/app
    command: tail -f /dev/null

  prometheus:
    image: prom/prometheus:v2.49.1
    container_name: prometheus
    ports:
      - "9090:9090"
    logging: *default-logging
    volumes:
      - ./init/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --web.enable-lifecycle
    extra_hosts:
      - "host.docker.internal:host-gateway" 

  grafana:
    image: grafana/grafana:10.2.3
    container_name: grafana
    ports:
      - "3000:3000"
    logging: *default-logging
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD}
    volumes:
      - grafana_data:/var/lib/grafana
      - ../../grafana/provisioning:/etc/grafana/provisioning:ro
      - ../../grafana/dashboards:/var/lib/grafana/dashboards/transitflow:ro
  
volumes:
  redpanda_data:
  postgres_data:
  redis_data:
  minio_data:
  grafana_data: